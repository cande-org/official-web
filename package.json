{
  "name": "cande-homepage",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node scripts/serve.mjs",
    "build": "node scripts/build.mjs",
    "preview": "node scripts/serve.mjs --dist",
    "check": "node --check main.js && node --check scripts/serve.mjs && node --check scripts/build.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
